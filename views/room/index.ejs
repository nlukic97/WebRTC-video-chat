<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zoom clone</title>
    <link rel="stylesheet" href="style.css">
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
    <script>
        const ROOM_ID = '<%= roomId %>'
    </script>
</head>
<body>
    <!-- <h1>Whats up sir</h1>
        
        <% if (typeof (roomId) == 'undefined') { %>
            <% } else { %>
                <h2>Room id: <%= roomId %></h2>
                <% } %> -->
    <div id="video-grid">
        <button id="disconnectPeer">Peer disconnect</button>
        <button id="destroyPeer">Peer destroy</button>
    </div>          
    <script src="script.js?v=1243"></script> <!-- So we defined the static files of the server so we can use it here-->
</body>
</html>